spring:
  application:
    name: engine
  datasource:
    url: jdbc:sqlserver://localhost\SQLEXPRESS;databaseName=EsportsTournamentDB;encrypt=true;trustServerCertificate=true
    username: ducvu1
    password: 123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.SQLServerDialect

server:
  port: 8081
